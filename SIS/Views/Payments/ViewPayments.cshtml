@model SIS.Models.Payments.PaymentHistoryViewModel

@{
    ViewData["Title"] = "Financial Statement";
}

<div class="min-h-screen bg-slate-50 p-6">
    <!-- Header Card -->
    <div class="bg-white rounded-xl shadow-sm p-6 mb-6">
        <div class="flex flex-col md:flex-row justify-between items-start md:items-center gap-4">
            <div class="flex items-start gap-4">
                <i class="material-icons text-3xl text-cyan-950">history</i>
                <div>
                    <h1 class="text-2xl font-bold text-slate-800 mb-2">Payment History</h1>
                    <div class="text-slate-600">
                        <p><span class="font-medium">Student:</span> @Model.StudentName</p>
                        <p><span class="font-medium">ID:</span> @Model.StudentId</p>
                    </div>
                </div>
            </div>
            <div class="flex items-center gap-2 text-slate-600">
                <i class="material-icons">calendar_month</i>
                <span>Academic Year: @Model.AcademicYear</span>
            </div>
        </div>
    </div>

    <!-- Payment Summary Card -->
    <div class="bg-white rounded-xl shadow-sm p-6 mb-6">
        <div class="flex flex-col md:flex-row justify-between items-start md:items-center gap-6">
            <div class="flex items-center gap-4">
                <div class="bg-cyan-950 p-4 rounded-xl">
                    <i class="material-icons text-2xl text-white">payments</i>
                </div>
                <div>
                    <h3 class="text-sm font-medium text-slate-600">Total Paid</h3>
                    <p class="text-2xl font-bold text-cyan-950">ZMW @Model.TotalPaid.ToString("N2")</p>
                </div>
            </div>
            <a href="@Url.Action("StudentFinance")"
               class="flex items-center gap-2 px-4 py-2 border border-cyan-950 text-cyan-950 rounded-lg hover:bg-cyan-950 hover:text-white transition-colors">
                <i class="material-icons">arrow_back</i>
                <span>Back to Fee Statement</span>
            </a>
        </div>
    </div>

    <!-- Transactions Table Card -->
    @* <div class="bg-white rounded-xl shadow-sm">
        <div class="p-6 border-b border-slate-200">
            <div class="flex items-center gap-2">
                <i class="material-icons text-cyan-950">format_list_bulleted</i>
                <h2 class="text-lg font-semibold text-slate-800">Transaction History</h2>
            </div>
        </div>
        <div class="p-6">
            @if (Model.Payments.Any())
            {
                <div class="overflow-x-auto">
                    <table class="w-full">
                        <thead class="bg-slate-50">
                            <tr>
                                <th class="px-6 py-3 text-left text-sm font-semibold text-slate-800">Date</th>
                                <th class="px-6 py-3 text-left text-sm font-semibold text-slate-800">Reference</th>
                                <th class="px-6 py-3 text-left text-sm font-semibold text-slate-800">Method</th>
                                <th class="px-6 py-3 text-right text-sm font-semibold text-slate-800">Amount (ZMW)</th>
                                <th class="px-6 py-3 text-right text-sm font-semibold text-slate-800">Balance (ZMW)</th>
                            </tr>
                        </thead>
                        <tbody class="divide-y divide-slate-200">
                            @foreach (var payment in Model.Payments)
                            {
                                <tr class="hover:bg-slate-50">
                                    <td class="px-6 py-4 text-sm text-slate-600">
                                        @payment.PaymentDate.ToString("MMM dd, yyyy HH:mm")
                                    </td>
                                    <td class="px-6 py-4 text-sm text-slate-600">
                                        @payment.TransactionReference
                                    </td>
                                    <td class="px-6 py-4 text-sm">
                                        <span class="px-2 py-1 bg-green-100 text-green-800 rounded-full text-xs font-medium">
                                            @payment.PaymentMethod
                                        </span>
                                    </td>
                                    <td class="px-6 py-4 text-sm text-green-600 text-right font-medium">
                                        @payment.AmountPaid.ToString("N2")
                                    </td>
                                    <td class="px-6 py-4 text-sm text-red-600 text-right font-medium">
                                        @payment.OutstandingAmount.ToString("N2")
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            else
            {
                <div class="text-center py-12">
                    <i class="material-icons text-5xl text-slate-400 mb-4">receipt_long</i>
                    <p class="text-slate-600">No payment records found.</p>
                </div>
            }
        </div>
    </div> *@
    <!-- Payment History -->
    @if (Model.FinancialStatement.Any())
    {
        <div class="bg-white rounded-lg border border-slate-200 overflow-hidden">
            <div class="bg-slate-50 px-6 py-4 border-b border-slate-200">
                <h3 class="text-lg font-semibold text-slate-800 flex items-center">
                    <i class="material-icons text-primary-600 mr-2">history</i>
                    Financial Statement
                </h3>
            </div>
            <div class="overflow-x-auto">
                <table class="w-full text-sm text-left text-slate-700">
                    <thead class="text-xs uppercase bg-slate-50">
                        <tr>
                            <th class="px-6 py-3">Date</th>
                            <th class="px-6 py-3">Transaction Reference</th>
                            <th class="px-6 py-3">Credit</th>
                            <th class="px-6 py-3">Debit</th>
                            <th class="px-6 py-3">Running Balance</th>
                        </tr>
                    </thead>
                    <tbody>
                        @{
                            decimal runningBalance = 0;
                        }
                        @foreach (var payment in Model.FinancialStatement)
                        {
                            if (payment.Credit)
                            {
                                runningBalance -= payment.Amount.Value;
                            }
                            else
                            {
                                runningBalance += payment.Amount.Value;
                            }
                            <tr class="bg-white border-b hover:bg-slate-50">
                                <td class="px-6 py-4">
                                    @payment.CreatedAt.ToString("MMM dd, yyyy")
                                    <div class="text-xs text-slate-500">@payment.CreatedAt.ToString("HH:mm")</div>
                                </td>
                                <td class="px-6 py-4 font-mono text-xs">@payment.Reference</td>
                                <td class="px-6 py-4 font-semibold text-green-600">
                                    @if (payment.Credit)
                                    {
                                        <span>K @payment.Amount.Value.ToString("N2")</span>
                                    }
                                    else
                                    {
                                        <span>K 0.00</span>
                                    }
                                </td>
                                <td class="px-6 py-4 font-semibold text-red-600">
                                    @if (!payment.Credit)
                                    {
                                        <span>K @payment.Amount.Value.ToString("N2")</span>
                                    }
                                    else
                                    {
                                        <span>K 0.00</span>
                                    }
                                </td>
                                @if (runningBalance < 0)
                                {
                                    <td class="px-6 py-4 font-semibold text-green-600">
                                        @((runningBalance < 0 ? $"({Math.Abs(runningBalance).ToString("N2")})" : runningBalance.ToString("N2")))
                                    </td>
                                }
                                else
                                {
                                    <td class="px-6 py-4 font-semibold text-red-600">
                                        @runningBalance.ToString("N2")
                                    </td>
                                }
                                @* <td class="px-6 py-4">
                                    <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-blue-100 text-blue-800">
                                        @payment.AccountingSystemPostStatus
                                    </span>
                                </td>
                                <td class="px-6 py-4 font-semibold text-slate-800">K @payment.OutstandingAmount.ToString("N2")</td> *@
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    }
    else
    {
        <div class="bg-white rounded-lg border border-slate-200 overflow-hidden">
            <div class="bg-slate-50 px-6 py-4 border-b border-slate-200">
                <h3 class="text-lg font-semibold text-slate-800 flex items-center">
                    <i class="material-icons text-primary-600 mr-2">history</i>
                    Payment History
                </h3>
            </div>
            <div class="p-6 text-center">
                <i class="material-icons text-6xl text-slate-300 mb-4">payment</i>
                <p class="text-slate-500 text-lg font-medium">No Payment History</p>
                <p class="text-slate-400 text-sm mt-2">No payments have been made for this academic year</p>
            </div>
        </div>
    }

</div>